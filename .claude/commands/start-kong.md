Run the Kong startup script to bring up a local Kong instance via vkdr.

Execute `workspace/kong-tools/scripts/start-kong.sh` and report the result. After it completes, verify Kong is running by curling `http://manager.localhost:8000/status`.
