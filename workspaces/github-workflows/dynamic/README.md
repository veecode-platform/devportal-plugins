# Dynamic testing

After releasing a dynamic version of thisplugin, it is important to test it in a real environment.

The docker-compose file will dynamic load the plugin from the registry.

## Get the current version's integrity

```bash
./run-dynamic.sh
```
