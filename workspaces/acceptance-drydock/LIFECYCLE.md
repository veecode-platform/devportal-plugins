# acceptance-drydock workspace lifecycle

This workspace was created from the repository workspace template.

1. Develop the product under `plugins/`.
2. Run the role-sized proof 1 harness.
3. Run `yarn dev:dynamic` and prove the export in `devportal-local`.
4. Register and publish the candidate through `export-overlays`.
